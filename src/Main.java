import Alghoritms.*;
import GUI.*;

import javax.swing.*;

public class Main {
    public static void main(String[] args){
       // int[] c = {6,2,1,4,7,8,9,3,5};
        //InsertionSort is = new InsertionSort(c, new ArrayDrafter(c));
       // is.sortOne(2);
      //  is.sortOne(3);
       // is.printArray();
        UI ui = new UI();
        SwingUtilities.invokeLater(ui);
       // is.printArray();
       // QuickSort qs = new QuickSort(c);
        //qs.sort(0,c.length-1);
        
       // System.out.println(qs.iteration(0,8));
       // qs.sort(0,8);
        //System.out.println(qs.isPartOfArraySorted(0,2));
        //qs.printArray();
    }
}
